# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage
/test-results/
/playwright-report/
/blob-report/

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local
.env.local
.env.development
.env.production

# prisma
/prisma/*.db

# local object storage (lib/storage local provider)
/.storage/
/supabase/.temp/

# vercel
.vercel

# typescript
*.tsbuildinfo

# editor / AI tooling
.github/instructions/codacy.instructions.md
.qodo

# Stitch redesign — throwaway live verification screenshots (keep the design refs)
stitch-designs/_live-*.png
.gstack/

# Client commercial documents — budgets, cost breakdowns, pricing. These are
# working drafts that live alongside the repo, not part of the product, and a
# shared repo's history is permanent. Ignored so a broad `git add` cannot sweep
# them into a public commit. Force-add deliberately if one ever should ship.
#
# The prefix rule below was too narrow: CLIENT_COST_ESTIMATE.md is exactly the
# kind of document it exists to stop and was committed anyway, simply because it
# did not start with BLAK_MOH_. Match on what the file IS, not what it is called.
/BLAK_MOH_*
/CLIENT_COST_ESTIMATE.md
/*COST*.md
/*PRICING*.md
/*INVOICE*.md
/*QUOTE*.md

# Scratch directory from agent tooling runs.
/.codex-tmp/

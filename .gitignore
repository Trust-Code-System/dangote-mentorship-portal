# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage
/test-results/
/playwright-report/
/blob-report/

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local
.env.local
.env.development
.env.production

# prisma
/prisma/*.db

# local object storage (lib/storage local provider)
/.storage/

# vercel
.vercel

# typescript
*.tsbuildinfo

# editor / AI tooling
.github/instructions/codacy.instructions.md
.qodo

# Stitch redesign — throwaway live verification screenshots (keep the design refs)
stitch-designs/_live-*.png
.gstack/

# Client commercial documents — budgets, cost breakdowns, pricing. These are
# working drafts that live alongside the repo, not part of the product, and a
# shared repo's history is permanent. Ignored so a broad `git add` cannot sweep
# them into a public commit. Force-add deliberately if one ever should ship.
/BLAK_MOH_*

# Scratch directory from agent tooling runs.
/.codex-tmp/
